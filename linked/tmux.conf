# スクロールしたい
set-window-option -g mouse on

# prefixの変更
unbind-key C-b
set-option -g prefix C-t
bind-key C-t send-prefix

# 256色表示
set -g default-terminal "screen-256color"

# https://raw.githubusercontent.com/altercation/solarized/master/tmux/tmuxcolors-256.conf
#### COLOUR (Solarized 256)
# default statusbar colors
set-option -g status-bg colour235 #base02
set-option -g status-fg colour136 #yellow
set-option -g status-attr default

# default window title colors
set-window-option -g window-status-fg colour244 #base0
set-window-option -g window-status-bg default
#set-window-option -g window-status-attr dim

# active window title colors
set-window-option -g window-status-current-fg colour166 #orange
set-window-option -g window-status-current-bg default
#set-window-option -g window-status-current-attr bright

# pane border
set-option -g pane-border-fg colour235 #base02
set-option -g pane-active-border-fg colour240 #base01

# message text
set-option -g message-bg colour235 #base02
set-option -g message-fg colour166 #orange

# pane number display
set-option -g display-panes-active-colour colour33 #blue
set-option -g display-panes-colour colour166 #orange

# clock
set-window-option -g clock-mode-colour colour64 #green
#### COLOUR (Solarized 256)

set-option -g default-command "which reattach-to-user-namespace > /dev/null && reattach-to-user-namespace -l $SHELL || $SHELL"

unbind-key -T copy-mode MouseDragEnd1Pane
unbind-key -T copy-mode M-w

if-shell 'which xsel' 'bind-key -t emacs-copy M-w copy-pipe "xsel -i -b"' 'bind-key -T copy-mode M-w send-keys -X copy-pipe-and-cancel "reattach-to-user-namespace pbcopy"'
if-shell 'which xsel' 'bind-key -t emacs-copy MouseDragEnd1Pane copy-pipe "xsel -i -b"' 'bind-key -T copy-mode MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "reattach-to-user-namespace pbcopy"'

# TODO fix for macOS
bind-key C-y run "xsel -o | tmux load-buffer - ; tmux paste-buffer"
